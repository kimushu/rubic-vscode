{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "rubic",
            "request": "launch",
            "name": "Launch",
            "workspaceRoot": "${workspaceRoot}",
            "program": "${workspaceRoot}/small.rb"
        }
    ]
}