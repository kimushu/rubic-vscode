{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "JSON schema for .vscode/rubic.json file",
  "type": "object",
  "required": [
    "boardId"
  ],
  "properties": {
    "boardId": {
      "description": "An identifier of board",
      "type": "string"
    },
    "boardPath": {
      "description": "A path of board (When VCP: \"COMxx\" for Windows, \"/dev/xx\" for Linux and Mac)",
      "type": "string"
    },
    "firmwareId": {
      "description": "An identifier of board firmware",
      "type": "string"
    },
    "transfer.include": {
      "description": "Files to be transfered to board. Accepts filename and glob patterns.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "transfer.exclude": {
      "description": "Files not to be transfered to board. Accepts filename and glob patterns. This is prior to 'include' field.",
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    }
  }
}